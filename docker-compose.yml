services:
  facebook-pages-mcp-community:
    build: .
    container_name: facebook-pages-mcp-community
    restart: unless-stopped
    ports:
      - "127.0.0.1:3038:3000"
    mem_limit: 512m
    mem_reservation: 256m
